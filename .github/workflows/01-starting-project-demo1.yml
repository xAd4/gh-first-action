name: Events Demo 1
on:
  push:
    branches:
      - main
      - "dev-*" # This will match any branch that starts with "dev-"
      - "dev/**" # This will match any branch that starts with "dev/" (e.g., "dev/feature1", "dev/bugfix2")
    paths:
      - "01 Starting Project/**"
  pull_request:
    types: [opened]
    paths:
      - "01 Starting Project/**"
    branches:
      - main
  workflow_dispatch:

defaults:
  run:
    working-directory: "./01 Starting Project"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Output event data
        run: echo "${{ toJSON(github.event) }}"
      - name: Install dependencies
        run: npm ci
      - name: Test code
        run: npm run test
      - name: Build code
        run: npm run build
      - name: Deploy project
        run: echo "Deploying..."
